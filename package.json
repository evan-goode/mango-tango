{
  "dependencies": {
    "@babel/polyfill": "^7.6.0",
    "parcel": "^1.12.4"
  },
  "scripts": {
    "start": "npx parcel source/index.html --cache-dir build/cache/ --out-dir build/",
    "build": "npx parcel build source/index.html --cache-dir build/cache/ --out-dir build/",
    "clean": "rm -rf build/*"
  }
}
